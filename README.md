# starlark-go-tutorial
Tutorial: Using Starlark as a Golang extension language.

# Practical Workshop: Starlark Integration with Golang

> **Note**: This workshop was generated by Perplexity AI as a learning resource. Feel free to adapt, modify, and share under the MIT License.

***

## Learning Objectives
By the end of this workshop, you will be able to:
- Execute Starlark scripts from Go applications
- Enable bidirectional communication between Go ↔ Starlark
- Create custom Go types and functions exposed to Starlark
- Handle errors and secure script execution
- Build a complete plugin system

***

## Prerequisites: Setup

**Task 0**: Configure your environment
1. Install the library: `go install go.starlark.net/cmd/starlark@latest`
2. Create a Go project: `go mod init starlark-workshop`
3. Import the package: `import "go.starlark.net/starlark"`
4. Verify the CLI interpreter works: `starlark` (then `Ctrl-D` to exit)

***

## Exercise 1: Execute Your First Starlark Script

**Context**: `starlark.ExecFile()` executes a Starlark file and retrieves globals.

### Steps
1. Create a file `greet.star` containing:
   - A global variable `message = "Hello from Starlark"`
   - A function `greet(name)` that returns a formatted greeting message

2. In `main.go`, write code to:
   - Create a `Thread` (Starlark execution context)
   - Execute `greet.star` using `starlark.ExecFileOptions()` (ExecFile is deprecated)
   - Retrieve the global `message` from the returned dictionary
   - Display this value in Go using `fmt.Printf()`

### Questions
- What type is returned by `ExecFileOptions()`?
- How do you handle execution errors?
- What happens if the file doesn't exist?

**Objective**: Master basic execution

***

## Exercise 2: Call a Starlark Function from Go

**Context**: `starlark.Call()` invokes a Starlark function with arguments.

### Steps
1. Reuse the `hello.star` file from Exercise 1
2. In `main.go`:
   - Retrieve the `greet` function from the globals dictionary
   - Call it with your first name as argument (use `starlark.String()` to convert)
   - Display the returned result

3. **Challenge**: Now call `greet` with 2 arguments (first name + last name)
   - Modify the Starlark function signature accordingly
   - Pass a `starlark.Tuple` containing the 2 arguments

### Questions
- How do you convert a Go string to `starlark.String`?
- What is the type of the result returned by `Call()`?
- How do you extract the Go value from a `starlark.Value`?

**Objective**: Invoke Starlark functions with arguments

***

## Exercise 3: Pass Go Data to Starlark

**Context**: You need to convert Go types to compatible Starlark types.

### Scenario
You have a Go struct representing a user:
```go
type User struct {
    Name  string
    Age   int
    Admin bool
}
```

### Steps
1. Create a file `user_info.star` with a function `describe_user(user_dict)` that:
   - Formats and returns a text description of the user
   - Handles the case where `user_dict["Admin"]` is `True`

2. In `main.go`:
   - Create a `User` instance
   - Convert this struct to `starlark.Dict` (use `starlark.NewDict()`)
   - Add key/value pairs with proper type conversions
   - Pass this dict to the Starlark function
   - Display the result

### Questions
- How do you convert a Go `int` to `starlark.Int`?
- How do you convert a Go `bool` to `starlark.Bool`?
- Which method do you use to add elements to a `starlark.Dict`?

**Objective**: Master Go → Starlark conversion

***

## Exercise 4: Retrieve Complex Data from Starlark

**Context**: Starlark can return complex structures (lists, dicts) that you must interpret in Go.

### Steps
1. Create `data_processor.star` with a function `process_numbers(nums)` that:
   - Receives a list of numbers
   - Returns a dict containing `{"sum": ..., "avg": ..., "max": ...}`

2. In `main.go`:
   - Create a `starlark.List` containing `[10, 20, 30, 40, 50]`
   - Call the Starlark function
   - Retrieve the returned dict
   - Extract each value (sum, avg, max) and convert them to Go types
   - Display the results

### Questions
- How do you iterate over a `starlark.Dict` from Go?
- How do you convert a `starlark.Int` to Go `int64`?
- How do you handle the case where an expected key doesn't exist in the dict?

**Objective**: Master Starlark → Go conversion

***

## Exercise 5: Create a Go Function Exposed to Starlark

**Context**: You can expose native Go functions to Starlark via `starlark.Builtin`.

### Steps
1. In `main.go`, create a Go function `log_message` that:
   - Takes a string argument from Starlark
   - Displays this message in the Go console with a timestamp
   - Returns `starlark.None`

2. Wrap this function in a `starlark.NewBuiltin()`:
   - Define the exposed function name (e.g., `"log"`)
   - Handle argument retrieval and validation with `UnpackArgs()` or `UnpackPositionalArgs()`

3. Create a script `script.star` that:
   - Calls your function `log("Hello from Starlark!")`

4. In `main.go`:
   - Create a `predeclared` dict containing your builtin function
   - Pass this dict as the 4th argument to `ExecFile()`

### Questions
- How do you validate the number and type of arguments in your Go function?
- What happens if Starlark passes the wrong type of argument?
- How do you return a Starlark error from Go?

**Objective**: Expose Go functions to Starlark

***

## Exercise 6: Create a Custom Go Type for Starlark

**Context**: You can create Go types that implement `starlark.Value` to be used in Starlark.

### Scenario
You want to create a `Sensor` type exposed to Starlark with methods.

### Steps
1. Define a `Sensor` struct in Go:
   - Fields: `ID string`, `Temperature float64`
   - Implement the `starlark.Value` interface (methods: `String()`, `Type()`, `Freeze()`, `Truth()`, `Hash()`)

2. Add a `read()` method accessible from Starlark:
   - Implement the `starlark.HasAttrs` interface with `Attr(name string)`
   - Return a `starlark.Builtin` for the `read` method
   - `read()` should return the current temperature

3. Expose a constructor `new_sensor(id)` to Starlark:
   - Go function that creates a `Sensor` instance
   - Returns a `*Sensor` (which is a `starlark.Value`)

4. In `sensor.star`, write a script that:
   - Creates a sensor: `s = new_sensor("temp-01")`
   - Reads its value: `temp = s.read()`
   - Displays the result

### Questions
- Why are `String()` and `Type()` necessary?
- How do you handle immutability with `Freeze()`?
- How do you make your type hashable (usable as dict key)?

**Objective**: Create rich Go types exposed to Starlark

***

## Exercise 7: Advanced Error Handling

**Context**: Starlark errors should be informative and well-located.

### Steps
1. Modify your Go code to capture and analyze Starlark errors:
   - Retrieve the error from `ExecFile()` or `Call()`
   - Cast the error to `*starlark.EvalError` if possible
   - Display the full backtrace with `err.Backtrace()`

2. Create a script `buggy.star` intentionally containing:
   - A division by zero
   - Access to a non-existent key in a dict
   - A function call with wrong arguments

3. Execute each case and observe the error messages

### Questions
- How do you differentiate a parsing error from an execution error?
- How do you obtain the line number where the error occurred?
- How do you create a custom error from Go that includes position in the script?

**Objective**: Handle errors properly

***

## Exercise 8: Limit Execution (Security)

**Context**: A Starlark script can potentially loop indefinitely or consume too many resources.

### Steps
1. Configure a `Thread` with security hooks:
   - Use `Thread.SetMaxExecutionSteps()` (if available) to limit steps
   - Implement a timeout with a `context.Context` and goroutine
   - Cancel execution if it exceeds 5 seconds

2. Create a script `infinite.star` with an infinite loop:
   ```
   def infinite_loop():
       i = 0
       for _ in range(999999999):  # Note: Starlark doesn't have while
           i = i + 1
   ```

3. Execute this script and verify your timeout mechanism works

### Questions
- How do you properly interrupt a Starlark execution from Go?
- What other limits could you impose (memory, recursion depth)?
- How do you forbid certain dangerous built-in functions?

**Objective**: Secure execution of untrusted scripts

***

## Exercise 9: Modules and Imports

**Context**: Starlark supports `load()` to import modules.

### Steps
1. Create a module `utils.star` with:
   - Function `double(x)` that returns `x * 2`
   - Function `format_list(items)` that returns a formatted string

2. Create `main.star` that:
   - Loads the module: `load("utils.star", "double", "format_list")`
   - Uses these functions

3. In `main.go`, implement a `FileLoader`:
   - Implement the appropriate interface or use a custom loader
   - Pass it to the `Thread` via `thread.Load`
   - The loader should resolve and load `utils.star`

### Questions
- How do you handle caching of already-loaded modules?
- How do you restrict imports to a specific directory?
- Can you load modules from sources other than the filesystem (e.g., embedded, HTTP)?

**Objective**: Manage the module system

***

## Exercise 10: Inline Evaluation (Without Files)

**Context**: `starlark.Eval()` executes Starlark code directly from a string.

### Steps
1. Use `starlark.Eval()` to evaluate:
   - A simple expression: `"2 + 2"`
   - A function call: `"max([1, 5, 3])"`
   - A comprehension: `"[x*2 for x in range(5)]"`

2. Retrieve and display the results

3. **Challenge**: Create a mini-REPL (Read-Eval-Print-Loop) in Go that:
   - Reads a line from stdin
   - Evaluates it with `starlark.Eval()`
   - Displays the result
   - Repeats until `exit` or `Ctrl-D`

### Questions
- What's the difference between `Eval()`, `ExecFile()`, and `EvalExpr()`?
- How do you share an environment (variables) between multiple `Eval()` calls?
- Can you define functions with `Eval()`?

**Objective**: Execute Starlark code dynamically

***

## Exercise 11: Introspection and Debugging

**Context**: You can inspect Starlark values from Go for debugging.

### Steps
1. Create a script that returns a complex structure (dict of lists of dicts)

2. In Go, write a recursive function `printStarlarkValue(v starlark.Value, indent int)` that:
   - Detects the real type of `v` (Int, String, List, Dict, etc.)
   - Displays the value with indentation according to depth
   - Recurses on collections (List, Dict)

3. Use this function to pretty-print the returned structure

### Questions
- How do you differentiate a `starlark.Int` from a `starlark.String`?
- How do you iterate over all elements of a `starlark.List`?
- How do you retrieve all keys of a `starlark.Dict`?

**Objective**: Inspect and debug Starlark values

***

## Exercise 12: Mini-Project - IoT Plugin System

**Scenario**: You're developing an IoT gateway where users write Starlark scripts to process sensor data.

### Specifications
1. **Go API exposed to Starlark**:
   - `read_sensor(id)`: returns a dict `{"temperature": float, "humidity": float}`
   - `send_alert(message)`: sends an alert (print in Go for simplicity)
   - `log(level, message)`: log with level (INFO, WARN, ERROR)

2. **Custom types**:
   - Type `SensorData` with methods `.get_temp()`, `.get_humidity()`, `.is_critical()`

3. **User script** (`plugin.star`) should:
   - Read multiple sensors in a loop
   - Calculate average temperature
   - Send alert if average > threshold
   - Log operations

4. **Go constraints**:
   - Maximum 10-second timeout
   - Sandbox: no filesystem/network access (only via your API)
   - Proper error handling with backtrace

### Design Questions
- How would you organize your Go code (packages, architecture)?
- How would you unit test your exposed functions?
- How would you version exposed APIs for compatibility?
- How would you document the Starlark API for users?

**Objective**: Complete and realistic integration

***

## Resources to Validate Your Approach

- API Documentation: pkg.go.dev/go.starlark.net/starlark
- Official examples: `starlark/example_test.go` in the repo
- Starlark specification: github.com/google/starlark-go/doc/spec.md

## Bonus: Reflection Questions

1. **Performance**: How would you optimize execution of thousands of scripts per second?
2. **Security**: What attacks are possible and how would you prevent them?
3. **Versioning**: How would you handle breaking changes in your exposed Go API?
4. **Hot-reload**: How would you reload scripts without restarting the Go app?
5. **Distribution**: How would you package and distribute Starlark plugins?

Good luck with this workshop! 🚀

***

**License**: MIT  
**Generated by**: Perplexity AI  
**Date**: May 2026
